---
- name: Ansible test playbook for integration with Jenkins
  hosts: "{{ target }}"
  remote_user: uharov

  tasks:
  - name: Creates directory
    file:
      path: /tmp/www
      state: directory
